import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
  palette: {
    primary: {
      // light: '#7ad6dc',
      main: '#7ad6dc',
      // dark: '#7ad6dc',
      // contrastText: '#7ad6dc',
    },
    secondary: {
      // light: 'red',
      main: '#000',
      // dark: 'red',
      // contrastText: 'red',
    },
  },
});
