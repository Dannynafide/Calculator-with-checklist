import { evaluate } from 'mathjs';

export const isNumberError = (items) => {
  // for (let i = 0; i < items.length; i++) {
  //   items[i].error = true;
  //   console.log(items[i]);
  // }
  // console.log(items);
  // return items;
};

// if (!isFinite(expr) && isNaN(expr)) {
//     item.error = true;
//   } else {
//     item.error = false;
//   }
